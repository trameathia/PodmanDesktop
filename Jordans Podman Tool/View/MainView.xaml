<Window x:Class="Jordans_Podman_Tool.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Jordans_Podman_Tool.View"
        mc:Ignorable="d"
        Title="Jordan's Podman Tool" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:PodView Grid.Row="0" x:Name="podView"/>
        <local:ContainerView Grid.Row="1" x:Name="containerView"/>
        <local:ImageView Grid.Row="2" x:Name="imageView"/>
    </Grid>
</Window>
